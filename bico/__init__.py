"""bico - Binary Converter for ETH eddy covariance raw data.

Kept import-light on purpose: importing this package must not pull in PyQt5
(so the conversion modules and tests can be imported without the GUI). Use the
console entry point ``bico`` or ``python -m bico`` to run the application.
"""
